/**
 * Shared vault “architect” (background ingest) — distinct from the main chat assistant orb.
 * Yellow reads as “working / attention” on dark UI.
 */
export const ARCHITECT_ORB_COLORS: [string, string] = ["#FDE047", "#CA8A04"];

/** Main Hermes chat agent — same palette as conversation orbs. */
export const CHAT_AGENT_ORB_COLORS: [string, string] = ["#a3c4f3", "#6b8cce"];
